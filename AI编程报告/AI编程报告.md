

# AI编程报告

### AI工具：

Trae CN

### 工作流复盘：

在Trae的IDE中，将智能体调至agent模式，选择deepseek-v4-pro模型，与AI交流。

```
创建一个基于Vue 3框架的拼豆(Perler Beads)网页生产工具，集成AI图像处理功能，实现从图片上传到实体拼豆图纸生成的完整工作流程。该工具应具备以下核心功能和技术要求：

1. 项目基础架构：

- 使用Vue 3 + Vite构建前端项目，采用TypeScript进行类型管理

- 集成Pinia进行状态管理，Vue Router处理路由

- 实现响应式设计，确保在桌面端和 tablet 设备上均有良好体验

2. 核心功能模块：

a) 图片上传与预处理：

- 支持常见图片格式上传（JPG、PNG等）

- 提供图片预览、缩放和裁剪功能

- 实现非像素普通图片的自动像素化处理（可自定义像素宽度和高度参数，生成马赛克效果）

b) 色盘匹配系统：

- 内置至少3种主流拼豆品牌的基础色盘数据（如Perler、Artkal、Hama）

- 实现精确的颜色匹配算法，将图片像素颜色映射到最接近的实体拼豆色号

- 提供手动调整颜色匹配的功能，允许用户微调颜色选择

c) 图纸生成与导出：

- 生成带有清晰网格线和色号标记的标准拼豆图纸

- 支持多种导出格式（至少包含PNG图片和PDF文档）

- 提供图纸预览功能，支持缩放查看细节

d) 材料消耗清单（进阶功能）：

- 自动计算并生成各颜色拼豆的精确使用数量

- 支持清单导出为文本或表格格式

- 可根据材料清单生成简单的购物清单

3. AI集成要求：

- 集成AI图像处理API或本地模型，实现图片像素化和颜色优化

- 确保AI处理过程有明确的加载状态提示

- 提供处理参数调整选项，允许用户优化AI处理结果

4. 用户体验要求：

- 设计直观的操作流程，从上传到下载不超过3步核心操作

- 提供详细的使用说明和帮助文档

- 实现操作过程中的实时预览和反馈

5. 技术实现要点：

- 使用Canvas API或WebGL进行图像渲染和处理

- 实现高效的颜色匹配算法，确保处理速度和准确性

- 优化大图片处理的性能，避免浏览器崩溃

- 实现本地存储功能，保存用户的历史项目和偏好设置

6. 交付物：

- 完整的Vue 3项目源代码

- 项目构建和部署文档

- 功能测试报告和性能优化说明

- 示例拼豆图纸和材料清单输出结果

该工具应满足专业拼豆爱好者的实际生产需求，提供准确的颜色匹配和清晰的图纸输出，同时保持良好的用户体验和系统性能。
```

### 难点分析：

生成的图纸存在准确度低，模糊的问题。有尝试提升色卡库颜色丰富程度，但没有成功。

```
1. 显著提升生成图纸的准确性和清晰度，解决当前图纸模糊不清的问题，确保所有线条、文字和细节清晰可辨，达到专业图纸的视觉标准。

2. 修改网格大小的单位设置，将当前的px单位替换为两个明确的毫米单位选项：2.6mm和5mm，并将2.6mm设置为默认选项。

3. 修复图纸生成功能中关于色号和网格线的勾选状态问题，确保当用户取消勾选"色号"和"网格线"选项时，生成的图纸中不再显示色号信息和网格线。

4. 解决生成的PDF文件中材料消耗清单的乱码问题，确保所有文字内容（包括中文、英文及特殊符号）都能正确显示，无字符替换、缺失或错误显示的情况。
```



随后在网页gemini中搜索：将图片转换为拼豆图纸主流的几种实现方法，了解了简单方案为计算欧几里得距离，随后将相关代码复制给TraeCN

```
// 欧几里得距离匹配

function findClosestColor(pixelRGB, colorPalette) {

let minDistance = Infinity;

let closest = null;

for (const color of colorPalette) {

const distance = Math.sqrt(

Math.pow(pixelRGB.r - color.r, 2) +

Math.pow(pixelRGB.g - color.g, 2) +

Math.pow(pixelRGB.b - color.b, 2)

);

if (distance < minDistance) {

minDistance = distance;

closest = color;

}

}

return closest;

}使用这个算法匹配颜色
```

最后发现是没有在像素化设置像素宽度。
